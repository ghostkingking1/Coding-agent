{
  "name": "coding-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal native TypeScript coding agent execution loop.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/cli.ts",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\""
  }
}
